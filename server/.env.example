DATABASE_URL=postgresql://postgres:postgres@localhost:5432/twelfthman?schema=public
JWT_SECRET=dev_secret_change_me
PORT=4000
NODE_ENV=development

# Fixtures / providers (optional; server boots without them)
API_FOOTBALL_KEY=
ADMIN_TOKEN=change-me

# FotMob enrichment (best-effort; kill-switchable)
ENABLE_FOTMOB=false
FOTMOB_REQUESTS_PER_MINUTE=30
FOTMOB_TTL_LIVE_SECONDS=60
FOTMOB_TTL_FINISHED_SECONDS=86400

# Python worker (best-effort; kill-switchable)
ENABLE_WORKER=false
WORKER_BASE_URL=http://localhost:6001
WORKER_REQUESTS_PER_MINUTE=30
